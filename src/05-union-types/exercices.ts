/**
 * TODO:
 * - implement Exclude
 * - implement Extract
 * - transform a union of values into a union of structure
 * - type never: the empty union
 */

namespace one {
  /**
   * TODO:
   * - implement Exclude
   * - implement Extract
   * - transform a union of values into a union of structure?
   * - type never: the empty union
   */
}

/**
 * TODO:
 * - implement Exclude
 * - implement Extract
 * - transform a union of values into a union of structure?
 * - type never: the empty union
 */

namespace exercise_1 {}

namespace exercise_1 {
  /**
   * TODO:
   * - implement Exclude
   * - implement Extract
   * - transform a union of values into a union of structure?
   * - type never: the empty union
   */
}
